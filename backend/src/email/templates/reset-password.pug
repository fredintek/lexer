extends _base.pug

block content
  table.email-wrapper(role='presentation' width='100%' cellpadding='0' cellspacing='0')
    tr
      td
        table.email-container(role='presentation' width='100%' cellpadding='0' cellspacing='0')
          tr
            td.email-header
              table(role='presentation' width='100%' cellpadding='0' cellspacing='0')
                tr
                  td.logo-row
                    table(role='presentation' cellpadding='0' cellspacing='0')
                      tr
                        td.logo-mark
                          svg(width='20' height='20' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg')
                            path(d='M4 14L8 9L11 12L16 6' stroke='white' stroke-width='2.2' stroke-linecap='round' stroke-linejoin='round')
                        td(style='width:10px')
                        td.logo-text Bulls#[span .]Yatırım

          tr
            td.email-body
              p.badge Password Reset
              h1.email-title Reset your#[br] password
              p.email-subtitle
                | Hi #{user.fullname}, we received a request to reset the password for your account. Copy the token below to reset your password. This session expires in #[strong #{expirationTime}].

              p(style='font-size:12px; color:#0052FF; word-break:break-all; background:#f8faff; padding:12px 14px; border-radius:8px; font-family:monospace')
                = token

              .divider

          tr
            td.email-footer
              p.footer-brand Bulls Yatırım
              p.footer-text
                | © #{new Date().getFullYear()} Bulls Yatırım. All rights reserved.#[br]
                | This email was sent to #{user.email}